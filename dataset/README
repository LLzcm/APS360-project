Uploaded a final zip of the dataset as well, since github truncates directories with over 1000 files to 1000. This means our training dataset is missing 200 images per class, so a zip of the entire dataset is included. 
